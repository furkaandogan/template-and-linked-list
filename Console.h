#include <iostream>

class Console{
	public:
		static void Clear();
		static char ReadKey();
		static char ReadLine();
		static void Write(char word[]);
		static void WriteLine(char word[]);
};
